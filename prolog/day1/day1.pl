% Make a simple knowledge base. Represent some of your favorite books and authors.
% Find all books in your knowledge base written by one author.

writes(alice, a).
writes(alice, l).
writes(alice, i).
writes(alice, c).
writes(alice, e).
writes(bob, b).
writes(bob, o).

% wirtes(alice, Book).


% Make a knowledge base representing musicians and instruments. Also represent musicians and their genre of music.
% Find all musicians who play the guitar.

musician_instrument(alice, guitar).
musician_instrument(bob, guitar).
musician_genre(alice, a).

% musician_instrument(Musician, guitar).
